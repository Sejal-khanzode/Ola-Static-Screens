export const apiStatus = {
  LOADING: "loading" as string,
  SUCCEEDED: "succeeded" as string,
  FAILED: "failed" as string,
  IDLE: "idle" as string,
};

export const ViewMode = {
  START: 'Start Appointment' as string,
  EDIT: "Edit" as string,
  VIEW: "View" as string,
  DELETE: "Delete" as string,
  RESCHEDULE: "Reschedule" as string,
  ADD: "Add" as string,
  RESTORE: "Restore" as string,
  OPEN_CHART:'Open Chart' as string,
  CANCEL: "Cancel" as string,
};

