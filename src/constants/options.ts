export type Options = {
  key: string;
  value: string;
  hide?: boolean;
  description?: string;
  type?: string;
  child?: React.ReactElement;
  color?: string;
}[];
export type Options2 = { label: string; value: string; key: string }[];
  